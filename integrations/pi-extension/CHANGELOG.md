# Changelog

## 0.1.0

- Add native Pi `tool_call` enforcement with fail-closed Arbiter intercept and permit verification.
- Add verified-call outcome recording through `tool_result`.
- Add local runtime discovery, workload identity headers, wildcard tool protection, and `/arbiter` status.
