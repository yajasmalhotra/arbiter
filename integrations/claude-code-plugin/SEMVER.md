# Versioning

This plugin follows semantic versioning independently from the Arbiter server.

- Patch: fixes preserving hook, configuration, and enforcement behavior.
- Minor: backward-compatible lifecycle or configuration additions.
- Major: breaking hook, configuration, canonical-request, or default changes.
