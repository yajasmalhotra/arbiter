{
  "name": "@randromeda/arbiter-claude-code",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code plugin that enforces Arbiter guardrails before tool execution.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "test": "node --test ./test/*.test.js",
    "validate": "node --check ./scripts/hook.js && node --check ./src/config.js && node --check ./src/guardrail.js && node --check ./src/http.js && node --check ./src/markers.js"
  }
}
