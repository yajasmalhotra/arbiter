# Versioning

This package follows semantic versioning independently from the Arbiter server.

- Patch: fixes that preserve hooks, configuration, and enforcement behavior.
- Minor: backward-compatible configuration or OpenCode lifecycle support.
- Major: breaking hook, configuration, canonical-request, or default-policy changes.
